{% extends 'base.html.twig' %}

{% block title %}CategorieExercice index{% endblock %}

{% block body %}
    <h1>CategorieExercice index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_exercice in categorie_exercices %}
            <tr>
                <td>{{ categorie_exercice.id }}</td>
                <td>
                    <a href="{{ path('app_categorie_exercice_show', {'id': categorie_exercice.id}) }}">show</a>
                    <a href="{{ path('app_categorie_exercice_edit', {'id': categorie_exercice.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_exercice_new') }}">Create new</a>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}CategorieExercice{% endblock %}

{% block body %}
    <h1>CategorieExercice</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_exercice.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_exercice_index') }}">back to list</a>

    <a href="{{ path('app_categorie_exercice_edit', {'id': categorie_exercice.id}) }}">edit</a>

    {{ include('categorie_exercice/_delete_form.html.twig') }}
{% endblock %}
